"""Infrastructure adapters that are NOT tied to a single feature module
(cross-cutting clients: cache/Redis, future message brokers). Feature-specific
adapters stay in their module's infrastructure/ package.
"""
